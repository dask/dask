name: dask-expr
channels:
  - conda-forge
dependencies:
  - pytest
  - pytest-cov
  - dask  # overridden by git tip below
  - pyarrow
  - pandas>=2
  - pre-commit
  - pip:
      - git+https://github.com/dask/distributed
      - git+https://github.com/dask/dask

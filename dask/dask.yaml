temporary-directory: null     # Directory for local disk like /tmp, /scratch, or /local

dataframe:
  shuffle-compression: null  # compression for on disk-shuffling. Partd supports ZLib, BZ2, SNAPPY, BLOSC

array:
  svg:
    size: 120  # pixels
  optimization: dask.array.optimization.optimize

dataframe:
  optimization: dask.dataframe.optimize.optimize

bag:
  optimization: dask.bag.core.optimize

delayed:
  optimiztion: dask.delayed.optimize
